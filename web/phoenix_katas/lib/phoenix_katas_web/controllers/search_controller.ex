defmodule PhoenixKatasWeb.SearchController do
  # use PhoenixKatasWeb, :controller

  # def search(conn, %{"txt" => txt}) do
  #   render(conn, "search.html", txt: txt)
  # end

  # def search(conn, %{}) do
  #   render(conn, "search.html", txt: "")
  # end
end

#[pub]
expression -> f64
	=  n:number  { n }

/*
number -> f64
	= n:$([0-9]+ "." [0-9]+)   { n.parse::<f64>().unwrap() }
*/

number -> f64
    = #quiet<>  / #expected("digits")




/*
sum -> f64
	= l:product "+" r:product { l+r }
	/ product
product -> f64
	= l:atom "*" r:atom { l*r }
	/ atom
atom -> f64
	= number
	/ "(" v:sum ")" { v }
    / expression
number -> f64
	= n:$([0-9]+ "." [0-9]+) { n.parse().unwrap() }
*/
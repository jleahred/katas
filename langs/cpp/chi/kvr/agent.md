

* las clases tienen que ser CamelCase, las variables snake_case, nos namespace snake_case
* siempre que sea posible, las variables tienen que ser const
* declarar variables con const auto var = ...
* si la variable no es const, que se llame mut_
* si la variable va a ser el resultado, aunque sea mutalble, el nombre que sea result (sin mut_)
* los comentarios tienen que estar en inglés
* los mensajes en inglés
* los if/else while..., siempre con llaves, aunque sólo sea una línea
* El README.md en español
* las funciones y métodos, tienen que ser camel_case